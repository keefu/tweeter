$(document).ready(function() {
    $("textarea").on("input", () => {
    var count = $("textarea").val().length;
    
    }); 
//document.addEventListener("input", () => {
//    var count = $("textarea").val().length;
//    console.log(count);  
//    });
});
  
